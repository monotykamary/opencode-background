{
  "name": "@zenobius/opencode-background",
  "version": "0.1.0",
  "description": "Background Tasks Plugin for OpenCode",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "test": "bun test",
    "prepare": "bun run build",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "files": [
    "dist",
    "src/version.ts"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@opencode-ai/plugin": "1.0.83",
    "execa": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "bun-types": "latest"
  }
}
